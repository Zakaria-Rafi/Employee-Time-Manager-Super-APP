class SignInPOM {
    logo = "#logo"
    titleSelector = "#signInTitle"
    titleTxt = "Login to your TimeManager account"
    emailLabelSelector = "#emailLabel"
    emailLabelTxt = "Email"
    pwdLabelSelector = "#passwordLabel"
    pwdLabelTxt = "Password"
    emailInputSelector = "#emailInput"
    pwdInputSelector = "#passwordInput"
    connectBtnSelector = "#connectBtn"
    connectBtnTxt = "Connect"
    errorToast = "#signinErrorToast"
}

export default new SignInPOM()